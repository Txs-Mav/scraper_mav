name: 'Scraping Morin Sports & Marine (toutes les 2h, 6h-18h Montréal)'

# Cron toutes les 2h en plage de jour (10-22 UTC = 6h-18h Montréal EDT)
# DÉDIÉ à Morin Sports & Marine, aligné sur la fréquence du cron global
# `scraper-cron.yml` mais décalé en minute pour ne pas saturer Supabase.
# Aucun run la nuit (l'inventaire concessionnaire ne bouge pas).
#
# Auto-généré par scraper_usine.workflow_generator. Ne pas éditer à la main :
# régénérer via `python -m scraper_ai.scraper_usine.main <url>` ou via
# l'API d'approbation `/api/admin/scrapers/morin-sports/approve` du dashboard.
#
# Le script `scrape_single_site.py` skip automatiquement si le cache Supabase
# est < 55 min — donc aucun risque de doublon avec le cron orchestrateur
# global qui tourne aux heures paires (HH:00) sur la même plage horaire.

on:
  schedule:
    - cron: '37 10-22/2 * * *'
  workflow_dispatch:
    inputs:
      force:
        description: "Ignorer le cache de fraîcheur (--force)"
        type: boolean
        default: false

concurrency:
  group: scraper-morin-sports
  cancel-in-progress: false

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout du repo
        uses: actions/checkout@v5

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: scraper_ai/requirements.txt

      - name: Installer les dépendances Python
        run: |
          pip install -r scraper_ai/requirements.txt
          pip install requests supabase

      - name: Scraping Morin Sports & Marine
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        run: |
          python scripts/scrape_single_site.py \
            --slug morin-sports \
            ${{ inputs.force && '--force' || '' }}
